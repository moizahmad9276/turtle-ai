// English fallback — used when backend is down and no cache exists
// This ensures the site ALWAYS works even with no internet/backend

import { englishStrings } from "../../../backend/src/shared/englishStrings";

export default englishStrings;
